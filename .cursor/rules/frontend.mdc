---
alwaysApply: false
---

- Use shadcn ui and tailwindcss has much possible
- we have tailwind class -primary- that matches our primary brand color (e.g text-primary-500)
- Avoid installing ui dependencies in apps/dashboard or other apps. Instead install ui deps in packages/ui and import components from there. 