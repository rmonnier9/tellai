{
  "name": "workers",
  "scripts": {
    "dev": "pnpm link-env && wrangler dev src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts",
    "dotenv": "dotenv -e ../../.env.local",
    "create-queue": "wrangler queues create lovarank-generic-queue",
    "link-env": "ln -sf ../../.env .dev.vars"
  },
  "dependencies": {
    "hono": "^4.5.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "dotenv-cli": "^7.1.0",
    "wrangler": "^3.57.2"
  }
}
